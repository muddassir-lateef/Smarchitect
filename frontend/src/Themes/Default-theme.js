import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2e3f5e",
      light: "#F57663"
    },
    secondary: {
      main: '#F57663',
    },
    otherColor: {
      main: "#999"
    }
  }
})